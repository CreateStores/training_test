define([
    'jquery'
    ],
    function ($) {
        'use strict';
        /**
         * result: http://take.ms/KRVLu
         */
    return function(config, element) {
        alert('Full action: ' + config.full_action);
    }
});